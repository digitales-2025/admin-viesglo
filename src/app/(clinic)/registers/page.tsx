import { Shell, ShellHeader, ShellTitle } from "@/shared/components/layout/Shell";

export default function RegistersPage() {
  return (
    <Shell>
      <ShellHeader>
        <ShellTitle title="Registers" />
      </ShellHeader>
      hola mundo
    </Shell>
  );
}
